- Fixed Codex Session Viewer history pagination. The UI now requests
  `/api/sessions/:id/events?before=<uuid>&limit=200` when loading older
  messages instead of re-reading the same tail window.
- `serve-dashboard.py` now supports `before` cursors for Codex, Kimi, and
  regular UUID-backed sessions. Codex/Kimi `full=1` reads now scan from the
  beginning, so export/copy no longer starts from the large-file tail path.
- Removed the 500-event client-side trim that discarded older messages after
  loading history. Verified with `py_compile`, `git diff --check`, direct
  `SessionEventReader` counts on a large Codex JSONL, and HTTP calls against
  the restarted dashboard on port 37778.
- Follow-up UX fix: changed the history control from repeated manual
  pagination to one-click `Load Full History`, which internally fetches
  `before` batches of 500 until the start of the session.
