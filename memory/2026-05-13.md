# 2026-05-13

- Replaced the separate Session Viewer copy buttons with a single `Copy` dropdown in `dashboard.html`.
- Copy formats now offered from the dropdown: `Full`, `Chat`, `Full clean`, and `Full cleanest`.
- `Full clean` keeps the existing full-with-tools intent but minifies JSON instead of pretty-printing it, reducing line and character growth.
- `Full cleanest` further summarizes long tool calls/results while preserving more detail for error results.
- Added an `ID` button in the Session Viewer header to copy the currently viewed session ID; when viewing a subagent, it copies the subagent ID.
