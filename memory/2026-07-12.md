# 2026-07-12

- OpenClaw migrated current agent transcripts from direct `sessions/*.jsonl` files to per-agent `agent/openclaw-agent.sqlite` databases (schema version 6). Legacy JSONL files remain available but no longer receive current sessions.
- Added read-only SQLite session discovery and transcript reading to the Session Viewer while retaining legacy JSONL support. SQLite sessions support list ordering/status, event cursors, raw events, stats, inline subagents, and content search.
- Restarted the `cc-sesh-master` PM2 service and verified 427 OpenClaw sessions, including current July 12 sessions; the prior JSONL-only index exposed 409.
