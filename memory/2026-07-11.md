# 2026-07-11

- Diagnosed apparent duplicate Codex sessions in Session Viewer: they are a parent Codex session plus real `thread_source=subagent` rollout files. The Codex scanner indexes every `rollout-*.jsonl` as a top-level session and does not retain `parent_thread_id`/`agent_path`, while each child inherits the parent's first user prompt as its slug.
- Implemented the fix: the index now preserves Codex parent/subagent metadata, hides attached children from the main list, and exposes them through the existing subagent panel. Orphaned children remain listed so history is never lost; nested children are flattened under their nearest indexed root because the UI supports one level.
- Verified against the reported parent: it now lists once with `subagentCount: 2`; both children resolve through the events/stats API as `service_health (Kant)` and `recent_logs (Sartre)`.
